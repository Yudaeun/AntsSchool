providers = ["java"]

[variables]
JAVA_VERSION = "17"

[phases.build]
cmds = ["chmod +x gradlew && ./gradlew clean build -x test"]

[start]
cmd = "java -jar build/libs/AntsSchoolServer-0.0.1-SNAPSHOT.jar"
